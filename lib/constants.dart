import 'package:flutter/material.dart';

const kTextColor = Colors.blue;
const kOutPutColor = Colors.deepOrange;

const kHeightWidthText = TextStyle(
    color: kTextColor,
    fontSize: 60,
    fontWeight:FontWeight.bold
);

const kOutPutText = TextStyle(
    color: kOutPutColor,
    fontSize: 65,
    fontWeight:FontWeight.bold
);